[x] Reserve memory for variables (run.js 52  - doesn't work yet)
[*] Check example code if we can run it ld[axy] and st[axy] should work in runner!
[] We should ensure that odd bytes can work in asm code
[] Check variables and warn user if using vars out of memory or invalid spaces.
